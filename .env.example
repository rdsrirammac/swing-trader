# Copy to .env and fill in. Loaded by src/config.py via python-dotenv.
# Values here override config/settings.yaml's ${VAR:-default} placeholders.

POSTGRES_USER=swing
POSTGRES_PASSWORD=swing
POSTGRES_DB=swing_trader
DB_HOST=localhost
DB_PORT=5432

REDIS_HOST=localhost
REDIS_PORT=6379

MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_PORT=5000

# Optional
NEWSAPI_API_KEY=
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=
EMAIL_PASSWORD=
EMAIL_TO=

APP_ENV=development
